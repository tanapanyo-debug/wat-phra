# ระบบฐานข้อมูลพระภิกษุ

โฟลเดอร์งาน: `D:\ฐานข้อมูลพระภิกษุ`  
**คนละระบบกับบัญชีวัด** (`D:\WatAccounting`) และ**ตรวจบัญชีวัด**

| | พระภิกษุ | บัญชีวัด | ตรวจบัญชี |
|---|---|---|---|
| โฟลเดอร์ | `D:\ฐานข้อมูลพระภิกษุ` | `D:\WatAccounting` | `D:\WatAccounting\ตรวจบัญชีวัด` |
| ฐาน | `wat_phra` | `wat_accounting` | `wat_audit` |
| พอร์ต | **4200** | 4000 | 4100 |

ดับเบิลคลิก `เปิดฐานข้อมูลพระภิกษุ.bat` แล้วเปิด http://localhost:4200


| | พระภิกษุ (ไฟล์นี้) | บัญชีวัด | ตรวจบัญชี |
|---|---|---|---|
| โฟลเดอร์ | `ฐานข้อมูลพระภิกษุ` | `D:\WatAccounting` | `ตรวจบัญชีวัด` |
| ฐาน | `wat_phra` | `wat_accounting` | `wat_audit` |
| พอร์ต | **4200** | 4000 | 4100 |

หน้ารวม banchiwat.com **พักไว้ก่อน** ไม่ปนข่าวสารหรือประชาสัมพันธ์เข้ามาในระบบนี้

## เปิดบนเครื่องนี้

ดับเบิลคลิก `เปิดฐานข้อมูลพระภิกษุ.bat` แล้วเปิด http://localhost:4200

บันทึกได้: รายชื่อ · หนังสือสุทธิ (รับเข้าสังกัด / ย้ายสังกัด เป็นประวัติ) · บัญชีจำพรรษารายปี (ทำทุกปี เช่น 2568)

## อัปขึ้น Render (แยกจากบัญชีวัดและตรวจบัญชี)

คนละบริการกับ `wat-accounting.onrender.com` และ `wat-audit.onrender.com` — คนละ repo คนละฐาน ห้ามอัปปน

1. สร้าง repo GitHub ชื่อประมาณ `wat-phra` แล้วอัปโฟลเดอร์นี้ (อย่าใส่ `server/.env`)
2. บน Render กด **New Blueprint Instance** เลือก repo นี้ หรือสร้าง **Web Service** + **PostgreSQL** ใหม่เอง
3. Build: `npm install --prefix server` · Start: `node server/server.js` · Health: `/api/health`
4. ฐานต้องชื่อประมาณ `wat_phra` — ห้ามใช้ Internal Database ของบัญชีวัดหรือตรวจบัญชี
5. Environment: `DATABASE_URL` จากฐานใหม่ · `TRUST_PROXY=1` · `PHRA_ADMIN_USER=admin` · `PHRA_ADMIN_PASSWORD` ตั้งเองหรือให้ Render สุ่ม แล้วดูค่าใน Environment
6. Deploy แล้วเข้า `https://wat-phra.onrender.com` (ชื่อจริงตามที่ Render ตั้ง) ล็อกอิน admin ครั้งแรก แล้วเปลี่ยนรหัส

ข้อมูลในเครื่องพอร์ต 4200 **ไม่ถูกยกขึ้นเน็ตอัตโนมัติ** — อัปขึ้นครั้งแรกเป็นฐานว่าง ต้องกรอกใหม่หรือนำเข้าภายหลัง
